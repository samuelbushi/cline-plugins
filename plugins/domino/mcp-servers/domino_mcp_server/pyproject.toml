[project]
name = "domino-mcp-server"
version = "0.1.0"
description = "Run jobs and sync DFS project files on the Domino data science platform."
requires-python = ">=3.11"
dependencies = [
 "mcp[cli]>=1.6.0",
 "requests",
 "python-dotenv"
]
